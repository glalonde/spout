// Parameters that define the game. These don't change at runtime.
#[derive(Debug)]
pub struct GameParams {
    viewport_width: u32,
    viewport_height: u32,
    level_width: u32,
    level_height: u32,
}
